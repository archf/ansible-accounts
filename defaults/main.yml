---

user_ssh_config_defaults:
  ControlPersist: 60s
  ControlMaster: auto
  ControlPath: '~/.ssh/controlmasters/%r@%h:%p'

# directory containing sensitive data
# it is used to contain ssh_keys
private_dir: "{{ playbook_dir }}/private"

users: []
req_groups: []
users_removed: []
groups_removed: []

# this is thel list of files to create symlinks to
dotfiles:
  - vimrc
  - bashrc
  - gitconfig
  - git_template
  - profile
  - tmux.conf
  - zshrc
  - zprofile
  - ansible.cfg
  - urlview
